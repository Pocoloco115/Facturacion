<?xml version="1.0" encoding="UTF-8"?>
<persistence xmlns="http://java.sun.com/xml/ns/persistence"
             xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
             xsi:schemaLocation="http://java.sun.com/xml/ns/persistence http://java.sun.com/xml/ns/persistence/persistence_1_0.xsd"
             version="1.0">

    <persistence-unit name="default">
        <provider>org.hibernate.jpa.HibernatePersistenceProvider</provider>

        <!-- DataSource: cambia según tu configuración, o usa HSQLDB embebida -->
        <non-jta-data-source>java://comp/env/jdbc/facturacionDS</non-jta-data-source>

        <!-- Tus entidades reales -->
        <class>com.tuempresa.facturacion.modelo.Categoria</class>
        <class>com.tuempresa.facturacion.modelo.Producto</class>
        <class>com.tuempresa.facturacion.modelo.Cliente</class>
        <class>com.tuempresa.facturacion.modelo.Direccion</class>

        <properties>
            <!-- Hibernate crea/actualiza las tablas según tus entidades -->
            <property name="hibernate.hbm2ddl.auto" value="update"/>

            <!-- Configuración de SQL logging opcional -->
            <property name="hibernate.show_sql" value="true"/>
            <property name="hibernate.format_sql" value="true"/>
        </properties>
    </persistence-unit>

    <!-- Unidad para pruebas JUnit (opcional) -->
    <persistence-unit name="junit">
        <provider>org.hibernate.jpa.HibernatePersistenceProvider</provider>
        <properties>
            <property name="hibernate.connection.driver_class" value="org.hsqldb.jdbcDriver"/>
            <property name="hibernate.connection.url" value="jdbc:hsqldb:hsql://localhost:1666"/>
            <property name="hibernate.connection.username" value="sa"/>
            <property name="hibernate.connection.password" value=""/>
            <property name="hibernate.hbm2ddl.auto" value="create-drop"/>
        </properties>
    </persistence-unit>

</persistence>
